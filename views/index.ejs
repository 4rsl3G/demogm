<%
const otpModalBody = `
  <form method="post" action="/connect/otp/verify" class="row" data-loading="1" id="otpVerifyForm">
    <input type="hidden" name="otp" id="otpHidden" />

    <div class="split" style="justify-content:space-between; align-items:flex-start">
      <div>
        <div style="font-weight:950;font-size:13.5px; display:flex; align-items:center; gap:8px">
          <i class="ri-key-2-line"></i> Masukkan OTP (6 digit)
        </div>
        <div class="muted" style="margin-top:2px">
          Auto-submit saat digit ke-6 terisi. Bisa paste OTP sekaligus.
        </div>
      </div>
      <div class="otp-timer" title="Waktu berlaku OTP">
        <i class="ri-timer-line"></i>
        <span id="otpCountdown">--:--</span>
      </div>
    </div>

    <div class="otp-grid" id="otpGrid" aria-label="OTP input">
      <input class="otp-box" inputmode="numeric" maxlength="1" autocomplete="one-time-code" />
      <input class="otp-box" inputmode="numeric" maxlength="1" />
      <input class="otp-box" inputmode="numeric" maxlength="1" />
      <input class="otp-box" inputmode="numeric" maxlength="1" />
      <input class="otp-box" inputmode="numeric" maxlength="1" />
      <input class="otp-box" inputmode="numeric" maxlength="1" />
    </div>

    <div class="otp-hint" id="otpHint">Klik Request OTP dulu.</div>

    <div class="actions">
      <button class="btn primary" type="submit" id="otpVerifyBtn">
        <i class="ri-check-double-line"></i> Verify
      </button>
      <button class="btn secondary" type="button" id="otpClearBtn">
        <i class="ri-delete-back-2-line"></i> Clear
      </button>
    </div>

    <div class="muted" style="margin-top:8px">
      <i class="ri-information-line"></i>
      OTP token disimpan sementara di cookie <b>httpOnly</b> terenkripsi.
    </div>
  </form>
`;
%>

<%-
  include("layout", {
    title: "Connect",
    toast,
    otp_modal_body: otpModalBody,
    body: `
      <div class="grid">

        <div class="card">
          <div class="title"><i class="ri-plug-2-line"></i> Connect</div>
          <div class="muted">
            <b>Note:</b> ${note}<br/>
            Metode login: Email/Password atau OTP. Setelah connect, lanjut ke dashboard untuk <b>Merchant</b> & <b>Mutasi</b>.
          </div>

          <div style="height:12px"></div>

          <div class="card soft" style="padding:12px">
            <div class="split" style="gap:10px">
              <div style="flex:1; min-width:180px">
                <div style="font-weight:950;font-size:13.5px;display:flex;align-items:center;gap:8px">
                  <i class="ri-shield-check-line"></i> Aman
                </div>
                <div class="muted" style="margin-top:2px">Credential tidak disimpan.</div>
              </div>
              <div style="flex:1; min-width:180px">
                <div style="font-weight:950;font-size:13.5px;display:flex;align-items:center;gap:8px">
                  <i class="ri-cursor-line"></i> Manual
                </div>
                <div class="muted" style="margin-top:2px">Mutasi fetch manual.</div>
              </div>
            </div>
          </div>

          <div style="height:12px"></div>

          <div class="tabs">
            <button type="button" class="tab active" onclick="setTab('email')" data-tabbtn="email">
              <i class="ri-mail-line"></i> Email
            </button>
            <button type="button" class="tab" onclick="setTab('otp')" data-tabbtn="otp">
              <i class="ri-message-2-line"></i> OTP
            </button>
          </div>

          <div style="height:12px"></div>

          <!-- EMAIL -->
          <div data-tab="email">
            <form method="post" action="/connect/email" class="row" data-loading="1" autocomplete="on">
              <div class="row" style="gap:8px">
                <label class="muted" style="font-weight:900">Email</label>
                <input class="inp" name="email" placeholder="email@domain.com" autocomplete="username" />
              </div>

              <div class="row" style="gap:8px">
                <label class="muted" style="font-weight:900">Password</label>
                <div style="position:relative">
                  <input class="inp" id="pw" type="password" name="password" placeholder="••••••••" autocomplete="current-password" />
                  <button type="button" class="btn secondary" id="pwToggle"
                    style="position:absolute; right:8px; top:50%; transform:translateY(-50%); height:36px; padding:0 10px; border-radius:12px; width:auto">
                    <i class="ri-eye-line"></i>
                  </button>
                </div>
              </div>

              <div class="actions">
                <button class="btn primary" type="submit"><i class="ri-link-m"></i> Connect</button>
                <a class="btn secondary" href="/dashboard"><i class="ri-dashboard-line"></i> Dashboard</a>
              </div>

              <div class="muted">
                <i class="ri-lock-line"></i> Password dipakai untuk login lalu dibuang (tidak disimpan).
              </div>
            </form>
          </div>

          <!-- OTP -->
          <div data-tab="otp" style="display:none" id="otp">
            <form method="post" action="/connect/otp/request" class="row" data-loading="1" id="otpRequestForm" autocomplete="on">
              <div class="row" style="gap:8px">
                <label class="muted" style="font-weight:900">Nomor HP</label>
                <div class="split" style="flex-wrap:nowrap">
                  <input class="inp" style="max-width:120px" name="countryCode" placeholder="62" value="62" />
                  <input class="inp" name="phone" placeholder="812xxxxxxx (tanpa 0 depan, tanpa +)" inputmode="numeric" />
                </div>
                <div class="muted">Format disarankan: <b>812xxxxxxx</b></div>
              </div>

              <div class="actions">
                <button class="btn primary" type="submit"><i class="ri-send-plane-2-line"></i> Request OTP</button>
                <a class="btn secondary" href="/dashboard"><i class="ri-dashboard-line"></i> Dashboard</a>
              </div>

              <div class="muted">
                Setelah request sukses, modal OTP akan tampil. Token tidak ditampilkan ke user.
              </div>
            </form>
          </div>
        </div>

        <div class="card">
          <div class="title"><i class="ri-shield-check-line"></i> Privacy & Status</div>

          <div class="muted">
            <ul style="margin:0; padding-left:16px; line-height:1.75">
              <li><b>Credential tidak disimpan</b> (email/password/OTP).</li>
              <li>Session token terenkripsi di cookie <b>httpOnly</b> (sementara).</li>
              <li>Flow: Connect → Merchant → Mutasi.</li>
            </ul>
          </div>

          <div style="height:14px"></div>

          <div class="actions">
            <a class="btn secondary" href="/dashboard"><i class="ri-dashboard-line"></i> Open Dashboard</a>
            ${
              loggedIn
                ? `<span class="pill" style="display:inline-flex"><i class="ri-checkbox-circle-line"></i> Logged In</span>`
                : `<span class="pill" style="display:inline-flex"><i class="ri-close-circle-line"></i> Logged Out</span>`
            }
          </div>

          <div style="height:12px"></div>

          <div class="card soft" style="padding:12px">
            <div style="font-weight:950;font-size:13.5px; display:flex; align-items:center; gap:8px">
              <i class="ri-lightbulb-line"></i> Tips
            </div>
            <div class="muted" style="margin-top:4px">
              Jika nanti butuh lebih stabil, simpan session server-side (Redis/KV) agar tidak kena limit cookie.
            </div>
          </div>
        </div>
      </div>

      <script>
        (function(){
          const pw = document.getElementById("pw");
          const t = document.getElementById("pwToggle");
          if(!pw || !t) return;
          t.addEventListener("click", ()=>{
            const is = pw.getAttribute("type") === "password";
            pw.setAttribute("type", is ? "text" : "password");
            t.innerHTML = is ? '<i class="ri-eye-off-line"></i>' : '<i class="ri-eye-line"></i>';
          });
        })();

        (function(){
          if (location.hash === "#otp") window.setTab("otp");
        })();
      </script>
    `
  })
%>
